#!/usr/bin/python3
import random
number = random.randint(-10, 10)
match number:
    case number > 0:
        return number "is positive"
    case number == 0:
        return number "is zero"
    case _:
        return number "is negative"
